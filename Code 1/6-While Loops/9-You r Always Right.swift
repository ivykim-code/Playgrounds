//
while !isOnGem{
        while !isBlocked {
            moveForward()
            if isOnClosedSwitch {
                toggleSwitch()
            }
        };turnRight()
        
    };collectGem()


/*
 while is on close switch then toggle switch
 else moveForward
 continue until blocked
 when blocked turn right
 REPEAT AGAIN (2)
 TURN RIGHT in every corner
 */
